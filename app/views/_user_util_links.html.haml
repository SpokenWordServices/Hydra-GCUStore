- if current_user 
  = link_to "Log Out", logout_path 
  [
  = link_to current_user.login, user_path 
  ]
  - if current_user.last_search_url
    |
    = link_to('View your last search', catalog_index_path(current_user.last_search_url))
  | 
  = link_to 'Your Bookmarks', bookmarks_path  
  |
  = link_to "Saved Searches", saved_searches_path
- else 
  = link_to 'Login', login_path
|
= link_to "Search History", search_history_index_path 

=select_tag("add_asset", "<option selected=\"selected\">Create new...</option><option value=\"#{url_for(:action=>"new", :controller=>"assets", :content_type=>"hydrangea_article")}\">Add an article</option><option value=\"#{url_for(:action=>"new", :controller=>"assets", :content_type=>"hydrangea_dataset")}\">Add a dataset</option>", :onchange=>"if (this.value != '') { window.location = this.value; }")